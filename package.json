{
  "name": "openclaw-app",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "openclaw": "latest"
  },
  "scripts": {
    "heroku-postbuild": "npx -y node-prune && rm -rf .npm node_modules/.cache",
    "start": "openclaw onboard"
  }
}
