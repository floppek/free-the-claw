{
  "name": "openclaw-app",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "openclaw": "latest"
  },
  "scripts": {
    "heroku-postbuild": "npm prune --production && rm -rf node_modules/@types node_modules/.cache .npm && find node_modules -type f -name '*.map' -delete && find node_modules -type f -name '*.ts' -delete && find node_modules -type f -name '*.md' -delete",
    "start": "openclaw onboard"
  }
}
