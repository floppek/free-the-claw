{
  "name": "openclaw-app",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "openclaw": "latest"
  },
  "scripts": {
    "heroku-postbuild": "find node_modules -type f -name '*.map' -delete && find node_modules -type f -name '*.md' -delete && rm -rf .npm",
    "start": "openclaw onboard"
  }
}
