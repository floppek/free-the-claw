{
  "name": "openclaw-app",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "openclaw": "latest"
  },
  "scripts": {
    "postinstall": "npm cache clean --force && rm -rf /tmp/*",
    "start": "openclaw onboard"
  }
}

